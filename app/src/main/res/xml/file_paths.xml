<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internal files directory -->
    <files-path name="internal_files" path="." />

    <!-- External files directory specific to the app -->
    <external-files-path name="external_files" path="." />

    <external-path name="external_files" path="." />

    <!-- Access the root of external storage -->
    <external-path name="external_root" path="." />

    <!-- Specific directories for media files -->
    <external-files-path name="pictures" path="Pictures/" />  <!-- Accessing Pictures directory -->
    <external-files-path name="videos" path="Movies/" />      <!-- Accessing Movies directory -->

</paths>